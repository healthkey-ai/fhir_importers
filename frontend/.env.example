# Only used by the standalone dev harness (`npm run dev` → :5173), which plays
# the host role and points its axios client at the connector backend. When the
# remote is embedded in ht-phr, the host injects the apiClient, so this is
# unused there.
VITE_API_BASE_URL=http://localhost:9300
